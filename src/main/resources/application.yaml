spring:
  application:
    name: Craftly
    description: AI_CODE GENERATION Platform


  datasource:
    url: jdbc:postgresql://localhost:9000/testDB
    username: testUser
    password: password
    driver-class-name: org.postgresql.Driver


  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

jwt:
  secret-key: "your-256-bit-secret-key-for-dev-only-change-in-prod"
